# Fiche membre 360° — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adresse/CP/ville sur le compte joueur, édition admin de tout sauf l'email, et refonte de `/admin/members/[userId]` en cockpit 360° remplaçant le panneau latéral de la liste et les onglets.

**Architecture:** Migration additive sur `User` ; extension de `updateMembership` (écrit déjà `phone` sur User — même mouvement) et de `getMemberHistory` (participants nommés, résultat de match, à-venir, abonnement) ; côté front, la fiche devient un orchestrateur qui monte des cartes-composants dédiées, la liste navigue directement, `MemberPanel.tsx` disparaît.

**Tech Stack:** Prisma 7 (driver adapter, migrations SQL à la main + `db execute` en DEV), Express, Next.js 16, Jest (backend `node node_modules/jest/bin/jest.js`, frontend idem — shims `.bin` cassés sur ce poste), ts-jest sans type-check → `tsc --noEmit` en gate séparée.

**Spec:** `docs/superpowers/specs/2026-07-23-fiche-membre-360-design.md`

**Règles transverses:**
- Jamais `prisma migrate dev` ni `db push` (dérive de base DEV) : SQL additif + `prisma db execute`.
- Commits fréquents, ne JAMAIS committer les fichiers d'autrui (WIP parallèle possible) : toujours `git add` par chemins explicites.
- Après `prisma generate`, si le backend dev tourne : `touch src/app.ts` (le process garde l'ancien client sinon).

---

## File structure

**Backend**
- Modify: `backend/prisma/schema.prisma` — `User.address/postalCode/city`
- Create: `backend/prisma/migrations/20260723100000_add_user_address/migration.sql`
- Modify: `backend/src/services/club.service.ts` — `updateMembership` élargi + note auto
- Modify: `backend/src/routes/admin.ts` — PATCH `/members/:id` relaie les nouveaux champs
- Modify: `backend/src/routes/me.ts` — `PROFILE_SELECT` + PATCH `/api/me` (3 champs)
- Modify: `backend/src/services/memberStats.service.ts` — member élargi, participants nommés, match, `upcoming`, `subscription`
- Modify: `backend/src/content/legalVersions.ts` — bump PRIVACY

**Frontend**
- Modify: `frontend/lib/api.ts` — types `MemberHistory`/`UpdateMemberBody`/`MyProfile`
- Modify: `frontend/lib/memberStats.ts` — helpers purs (alertes, lignes de résa)
- Modify: `frontend/lib/meProfile.ts` + `frontend/components/profile/tabs/ProfileIdentity.tsx` — 3 champs joueur
- Modify: `frontend/app/admin/members/page.tsx` — navigation directe, panneau retiré
- Delete: `frontend/components/admin/members/MemberPanel.tsx`
- Create: `frontend/components/admin/members/MemberProfileCard.tsx` — profil éditable
- Create: `frontend/components/admin/members/MemberAccessCard.tsx` — rôle & accès + bloquer/supprimer
- Create: `frontend/components/admin/members/MemberWalletCard.tsx` — abonnement & soldes
- Create: `frontend/components/admin/members/MemberReservationsCard.tsx` — dernières réservations
- Create: `frontend/components/admin/members/MemberOverviewCards.tsx` — À venir, Paiements, Fidélité
- Modify: `frontend/app/admin/members/[userId]/page.tsx` — réécriture (hero + alertes + cartes + « plus »)
- Modify: `frontend/app/globals.css` — classes `.mb-*` (2 colonnes CSS pures)
- Modify: `frontend/lib/platformContent.ts` — tableau données + version PRIVACY

---

### Task 1: Migration `add_user_address`

**Files:**
- Modify: `backend/prisma/schema.prisma` (model User, après `avatarUrl`)
- Create: `backend/prisma/migrations/20260723100000_add_user_address/migration.sql`

- [ ] **Step 1: Ajouter les champs au schéma**

Dans `backend/prisma/schema.prisma`, model `User`, juste sous `avatarUrl` :

```prisma
  // Adresse postale du joueur (fichier adhérents — saisie par le joueur ou le staff du club).
  address               String?
  postalCode            String?   @map("postal_code")
  city                  String?
```

- [ ] **Step 2: Écrire la migration SQL**

Create `backend/prisma/migrations/20260723100000_add_user_address/migration.sql` :

```sql
-- Adresse postale du joueur (spec 2026-07-23-fiche-membre-360). Additif pur.
ALTER TABLE "users" ADD COLUMN IF NOT EXISTS "address" TEXT;
ALTER TABLE "users" ADD COLUMN IF NOT EXISTS "postal_code" TEXT;
ALTER TABLE "users" ADD COLUMN IF NOT EXISTS "city" TEXT;
```

- [ ] **Step 3: Appliquer en DEV + régénérer le client**

Run (depuis `backend/`) :
```bash
npx prisma db execute --file prisma/migrations/20260723100000_add_user_address/migration.sql
npx prisma generate
```
Expected : `Script executed` puis `Generated Prisma Client`. (Prisma 7 : la config vient de `prisma.config.ts`, pas de `--schema`.)

- [ ] **Step 4: Commit**

```bash
git add backend/prisma/schema.prisma backend/prisma/migrations/20260723100000_add_user_address/migration.sql
git commit -m "feat(members): colonnes adresse/CP/ville sur User (migration add_user_address)"
```

---

### Task 2: Backend — `updateMembership` élargi (identité + coordonnées, note auto)

**Files:**
- Modify: `backend/src/services/club.service.ts:739-758` (updateMembership)
- Modify: `backend/src/routes/admin.ts:418-423` (PATCH `/members/:id`)
- Test: `backend/src/services/__tests__/club.service.test.ts`

- [ ] **Step 1: Écrire les tests qui échouent**

Dans `backend/src/services/__tests__/club.service.test.ts`, ajouter un `describe` (réutiliser le harness de mock prisma du fichier — le bloc « updateMembership (garde staff sur status BLOCKED) » ligne ~1499 montre le pattern) :

```ts
describe('ClubService — updateMembership (profil élargi)', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    (prisma.clubMembership.findUnique as jest.Mock).mockResolvedValue({ clubId: 'club-demo', userId: 'u1' });
    (prisma.clubMembership.update as jest.Mock).mockResolvedValue({ id: 'mb1' });
    (prisma.user.update as jest.Mock).mockResolvedValue({ id: 'u1' });
    (prisma.memberNote.create as jest.Mock).mockResolvedValue({ id: 'n1' });
  });

  it('écrit prénom/nom/naissance/sexe/adresse/CP/ville sur le User', async () => {
    await new ClubService().updateMembership('club-demo', 'mb1', {
      firstName: ' Ines ', lastName: 'Andre', birthDate: '1992-09-04', sex: 'FEMALE',
      address: '12 rue des Sports', postalCode: '31000', city: 'Toulouse',
    });
    expect(prisma.user.update).toHaveBeenCalledWith(expect.objectContaining({
      where: { id: 'u1' },
      data: expect.objectContaining({
        firstName: 'Ines', lastName: 'Andre', sex: 'FEMALE',
        address: '12 rue des Sports', postalCode: '31000', city: 'Toulouse',
        birthDate: new Date('1992-09-04'),
      }),
    }));
  });

  it('refuse un prénom vide et une date invalide', async () => {
    const svc = new ClubService();
    await expect(svc.updateMembership('club-demo', 'mb1', { firstName: '  ' })).rejects.toThrow('VALIDATION_ERROR');
    await expect(svc.updateMembership('club-demo', 'mb1', { birthDate: '31/12/1990' })).rejects.toThrow('VALIDATION_ERROR');
    await expect(svc.updateMembership('club-demo', 'mb1', { sex: 'X' as never })).rejects.toThrow('VALIDATION_ERROR');
  });

  it('chaîne vide → null (adresse effacée), et ne touche pas le User sans champ User', async () => {
    await new ClubService().updateMembership('club-demo', 'mb1', { address: '  ', note: 'x' });
    expect(prisma.user.update).toHaveBeenCalledWith(expect.objectContaining({
      data: expect.objectContaining({ address: null }),
    }));
    (prisma.user.update as jest.Mock).mockClear();
    await new ClubService().updateMembership('club-demo', 'mb1', { note: 'y' });
    expect(prisma.user.update).not.toHaveBeenCalled();
  });

  it('trace la modification par une note automatique portée par le staff appelant', async () => {
    await new ClubService().updateMembership('club-demo', 'mb1', { phone: '0611009030', city: 'Toulouse' }, 'staff-1');
    expect(prisma.memberNote.create).toHaveBeenCalledWith({
      data: expect.objectContaining({
        clubId: 'club-demo', userId: 'u1', authorId: 'staff-1',
        body: expect.stringContaining('✎ Profil modifié'),
      }),
    });
  });

  it("un échec de la note automatique n'annule pas la mise à jour", async () => {
    (prisma.memberNote.create as jest.Mock).mockRejectedValue(new Error('db down'));
    await expect(new ClubService().updateMembership('club-demo', 'mb1', { phone: '06' }, 'staff-1')).resolves.toBeDefined();
  });
});
```

- [ ] **Step 2: Vérifier l'échec**

Run: `cd backend; node node_modules/jest/bin/jest.js --runTestsByPath src/services/__tests__/club.service.test.ts`
Expected: FAIL (les nouveaux champs sont ignorés, pas de note auto).

- [ ] **Step 3: Implémenter**

Remplacer `updateMembership` dans `backend/src/services/club.service.ts` :

```ts
  // Champs du COMPTE joueur (globaux, partagés entre clubs) éditables par le staff — l'email,
  // identifiant de connexion, n'en fait jamais partie. Libellés pour la note de traçage.
  private static readonly USER_FIELD_LABELS: Record<string, string> = {
    firstName: 'prénom', lastName: 'nom', phone: 'téléphone', birthDate: 'date de naissance',
    sex: 'sexe', address: 'adresse', postalCode: 'code postal', city: 'ville',
  };

  async updateMembership(
    clubId: string, membershipId: string,
    params: {
      isSubscriber?: boolean; membershipNo?: string | null; status?: 'ACTIVE' | 'BLOCKED'; note?: string | null;
      phone?: string | null; firstName?: string; lastName?: string;
      birthDate?: string | null; sex?: 'MALE' | 'FEMALE' | null;
      address?: string | null; postalCode?: string | null; city?: string | null;
    },
    actorUserId?: string,
  ) {
    const m = await prisma.clubMembership.findUnique({ where: { id: membershipId }, select: { clubId: true, userId: true } });
    if (!m || m.clubId !== clubId) throw new Error('MEMBER_NOT_FOUND');
    if (params.status === 'BLOCKED') await this.assertNotStaff(clubId, m.userId);

    // --- Champs du compte joueur (User global — même mouvement que phone historiquement) ---
    const userData: Record<string, unknown> = {};
    const touched: string[] = [];
    const optional = (key: 'phone' | 'address' | 'postalCode' | 'city') => {
      if (params[key] === undefined) return;
      userData[key] = params[key]?.toString().trim() || null;
      touched.push(key);
    };
    optional('phone'); optional('address'); optional('postalCode'); optional('city');
    for (const key of ['firstName', 'lastName'] as const) {
      if (params[key] === undefined) continue;
      const v = params[key]!.toString().trim();
      if (!v) throw new Error('VALIDATION_ERROR'); // un compte garde toujours un nom
      userData[key] = v; touched.push(key);
    }
    if (params.birthDate !== undefined) {
      if (params.birthDate === null) userData.birthDate = null;
      else {
        const s = String(params.birthDate);
        const parsed = /^\d{4}-\d{2}-\d{2}$/.test(s) ? new Date(s) : null;
        if (!parsed || isNaN(parsed.getTime()) || parsed.toISOString().slice(0, 10) !== s) throw new Error('VALIDATION_ERROR');
        userData.birthDate = parsed;
      }
      touched.push('birthDate');
    }
    if (params.sex !== undefined) {
      if (params.sex !== null && params.sex !== 'MALE' && params.sex !== 'FEMALE') throw new Error('VALIDATION_ERROR');
      userData.sex = params.sex; touched.push('sex');
    }
    if (touched.length > 0) {
      await prisma.user.update({ where: { id: m.userId }, data: userData });
      // Traçage : le journal de notes sert d'audit (best-effort, jamais bloquant).
      if (actorUserId) {
        const labels = touched.map((k) => ClubService.USER_FIELD_LABELS[k]).join(', ');
        await prisma.memberNote.create({
          data: { clubId, userId: m.userId, authorId: actorUserId, body: `✎ Profil modifié : ${labels}` },
        }).catch((e) => console.error('[members] note de traçage échouée:', e));
      }
    }

    return prisma.clubMembership.update({
      where: { id: membershipId },
      data: {
        ...(params.isSubscriber !== undefined ? { isSubscriber: params.isSubscriber } : {}),
        ...(params.membershipNo !== undefined ? { membershipNo: params.membershipNo?.toString().trim() || null } : {}),
        ...(params.status !== undefined ? { status: params.status } : {}),
        ...(params.note !== undefined ? { note: params.note?.toString().trim() || null } : {}),
      },
    });
  }
```

Et dans `backend/src/routes/admin.ts` (PATCH `/members/:id`, ligne ~418) :

```ts
router.patch('/members/:id', async (req: ClubScopedRequest, res: Response, next: NextFunction) => {
  try {
    const { isSubscriber, membershipNo, status, note, phone, firstName, lastName, birthDate, sex, address, postalCode, city } = req.body;
    res.json(await clubService.updateMembership(
      req.membership!.clubId, asString(req.params.id),
      { isSubscriber, membershipNo, status, note, phone, firstName, lastName, birthDate, sex, address, postalCode, city },
      req.user!.id,
    ));
  } catch (err) { handleError(err, res, next); }
});
```

Note : si le harness de test ne mocke pas `prisma.memberNote`, ajouter `memberNote: { create: jest.fn() }` au mock prisma en tête de fichier.

- [ ] **Step 4: Vérifier le vert**

Run: `cd backend; node node_modules/jest/bin/jest.js --runTestsByPath src/services/__tests__/club.service.test.ts`
Expected: PASS (y compris les anciens cas updateMembership).

- [ ] **Step 5: Commit**

```bash
git add backend/src/services/club.service.ts backend/src/routes/admin.ts backend/src/services/__tests__/club.service.test.ts
git commit -m "feat(members): edition admin identite+coordonnees (tout sauf email) + note de tracage"
```

---

### Task 3: Backend — le joueur édite ses nouveaux champs (`PATCH /api/me`)

**Files:**
- Modify: `backend/src/routes/me.ts` (`PROFILE_SELECT` ligne ~37, PATCH `/` ligne ~134)
- Test: `backend/src/routes/__tests__/me.routes.test.ts`

- [ ] **Step 1: Tests qui échouent**

Dans `me.routes.test.ts`, ajouter au bloc du PATCH profil (suivre le harness existant du fichier — supertest + mock prisma) :

```ts
it('PATCH /api/me accepte adresse/CP/ville (trim, vide → null)', async () => {
  const res = await request(app).patch('/api/me')
    .set('Authorization', `Bearer ${token}`)
    .send({ address: ' 12 rue des Sports ', postalCode: '31000', city: '' });
  expect(res.status).toBe(200);
  expect(prisma.user.update).toHaveBeenCalledWith(expect.objectContaining({
    data: expect.objectContaining({ address: '12 rue des Sports', postalCode: '31000', city: null }),
  }));
});
```

- [ ] **Step 2: Vérifier l'échec**

Run: `cd backend; node node_modules/jest/bin/jest.js --runTestsByPath src/routes/__tests__/me.routes.test.ts`
Expected: FAIL (`data` ne contient pas `address`).

- [ ] **Step 3: Implémenter**

Dans `backend/src/routes/me.ts` :
1. `PROFILE_SELECT` : ajouter `address: true, postalCode: true, city: true,` après `avatarUrl: true`.
2. Dans le PATCH `/` (ligne ~134), destructurer aussi `address, postalCode, city` du body et, à côté du traitement de `phone` :

```ts
for (const [key, v] of [['address', address], ['postalCode', postalCode], ['city', city]] as const) {
  if (v !== undefined) data[key] = typeof v === 'string' && v.trim() ? v.trim() : null;
}
```

- [ ] **Step 4: Vérifier le vert**

Run: `cd backend; node node_modules/jest/bin/jest.js --runTestsByPath src/routes/__tests__/me.routes.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add backend/src/routes/me.ts backend/src/routes/__tests__/me.routes.test.ts
git commit -m "feat(profile): adresse/CP/ville editables par le joueur (PATCH /api/me)"
```

---

### Task 4: Backend — `getMemberHistory` enrichi

**Files:**
- Modify: `backend/src/services/memberStats.service.ts`
- Test: `backend/src/services/__tests__/memberStats.service.test.ts`

- [ ] **Step 1: Tests qui échouent**

Ajouter au fichier de test (réutiliser les fixtures/mocks prisma existants du fichier ; compléter le mock prisma avec `tournamentRegistration.findMany`, `eventRegistration.findMany`, `lessonEnrollment.findMany`, `subscription.findFirst`, `clubMember.findUnique`, `coach.findFirst` s'ils manquent — tous `jest.fn().mockResolvedValue([])` / `null` par défaut) :

```ts
describe('getMemberHistory — enrichissements fiche 360', () => {
  it('expose membershipId, identité complète, rôle et facettes', async () => {
    (prisma.clubMembership.findUnique as jest.Mock).mockResolvedValue({
      id: 'mb1', createdAt: new Date('2024-03-01'), isSubscriber: true, membershipNo: 'PAR1010',
      status: 'ACTIVE', watch: false, isReferee: true, note: 'VIP',
      user: { firstName: 'Ines', lastName: 'Andre', email: 'i@a.fr', phone: '06', avatarUrl: null,
        isSuperAdmin: false, birthDate: new Date('1992-09-04'), sex: 'FEMALE',
        address: '12 rue des Sports', postalCode: '31000', city: 'Toulouse' },
    });
    (prisma.clubMember.findUnique as jest.Mock).mockResolvedValue({ role: 'STAFF' });
    (prisma.coach.findFirst as jest.Mock).mockResolvedValue(null);
    const h = await new MemberStatsService().getMemberHistory('club-demo', 'u1');
    expect(h.member).toMatchObject({
      membershipId: 'mb1', birthDate: '1992-09-04', sex: 'FEMALE',
      address: '12 rue des Sports', postalCode: '31000', city: 'Toulouse',
      staffRole: 'STAFF', isCoach: false, isReferee: true, note: 'VIP',
    });
  });

  it('les réservations portent participants nommés et résultat de match', async () => {
    (prisma.reservation.findMany as jest.Mock).mockResolvedValue([{
      id: 'r1', status: 'CONFIRMED', type: 'COURT', startTime: new Date('2026-07-19T16:00:00Z'),
      endTime: new Date('2026-07-19T17:30:00Z'), totalPrice: 25, cancelledAt: null, userId: 'u1',
      resource: { name: 'Terrain 2', price: 25, offPeakPrice: null, clubSport: { sport: { key: 'padel' } } },
      participants: [
        { id: 'p1', userId: 'u1', share: 0, isOrganizer: true, user: { firstName: 'Ines', lastName: 'Andre' } },
        { id: 'p2', userId: 'u2', share: 0, isOrganizer: false, user: { firstName: 'Lucas', lastName: 'Martin' } },
      ],
      payments: [],
      matches: [{ status: 'CONFIRMED', winningTeam: 1, sets: [[6, 3], [6, 4]], competitive: true,
        players: [{ userId: 'u1', team: 1 }] }],
    }]);
    const h = await new MemberStatsService().getMemberHistory('club-demo', 'u1');
    expect(h.reservations[0].participants).toEqual([
      { userId: 'u1', firstName: 'Ines', lastName: 'Andre', isOrganizer: true },
      { userId: 'u2', firstName: 'Lucas', lastName: 'Martin', isOrganizer: false },
    ]);
    expect(h.reservations[0].match).toEqual({ winningTeam: 1, myTeam: 1, sets: [[6, 3], [6, 4]], competitive: true });
  });

  it('upcoming fusionne résas futures, tournois, events et cours (tri asc, cap 5)', async () => {
    (prisma.tournamentRegistration.findMany as jest.Mock).mockResolvedValue([
      { status: 'CONFIRMED', tournament: { id: 't1', name: 'P100 Dames', startTime: new Date('2099-07-26T08:00:00Z') } },
    ]);
    (prisma.eventRegistration.findMany as jest.Mock).mockResolvedValue([
      { status: 'WAITLISTED', event: { id: 'e1', name: 'Mêlée du soir', startTime: new Date('2099-07-24T18:00:00Z') } },
    ]);
    (prisma.lessonEnrollment.findMany as jest.Mock).mockResolvedValue([
      { lesson: { id: 'l1', reservation: { startTime: new Date('2099-07-28T10:00:00Z'), resource: { name: 'Terrain 1' } } } },
    ]);
    const h = await new MemberStatsService().getMemberHistory('club-demo', 'u1');
    expect(h.upcoming.map((u) => u.kind)).toEqual(['event', 'tournament', 'lesson']);
    expect(h.upcoming[0]).toMatchObject({ kind: 'event', title: 'Mêlée du soir', status: 'WAITLISTED' });
  });

  it("expose l'abonnement actif (ou null)", async () => {
    (prisma.subscription.findFirst as jest.Mock).mockResolvedValue({
      id: 's1', planId: 'pl1', expiresAt: new Date('2099-08-10'), monthlyPriceSnapshot: 39, sportKeys: ['padel'],
      plan: { name: 'Padel illimité' },
    });
    const h = await new MemberStatsService().getMemberHistory('club-demo', 'u1');
    expect(h.subscription).toMatchObject({ id: 's1', planName: 'Padel illimité', monthlyPriceSnapshot: '39' });
  });
});
```

- [ ] **Step 2: Vérifier l'échec**

Run: `cd backend; node node_modules/jest/bin/jest.js --runTestsByPath src/services/__tests__/memberStats.service.test.ts`
Expected: FAIL.

- [ ] **Step 3: Implémenter**

Dans `backend/src/services/memberStats.service.ts` :

1. **Interfaces** — étendre `MemberHistoryReservation` et `MemberHistory` :

```ts
export interface MemberHistoryReservation {
  // ... champs existants inchangés ...
  participants: Array<{ userId: string; firstName: string; lastName: string; isOrganizer: boolean }>;
  match: { winningTeam: number | null; myTeam: number | null; sets: [number, number][]; competitive: boolean } | null;
}

export interface MemberUpcomingEntry {
  kind: 'reservation' | 'tournament' | 'event' | 'lesson';
  id: string;
  title: string;
  startTime: string;
  status: string | null; // CONFIRMED / WAITLISTED pour tournoi/event, null sinon
}

export interface MemberHistory {
  member: {
    // ... existants ... +
    membershipId: string;
    birthDate: string | null; sex: 'MALE' | 'FEMALE' | null;
    address: string | null; postalCode: string | null; city: string | null;
    staffRole: 'OWNER' | 'ADMIN' | 'STAFF' | null; isCoach: boolean; isReferee: boolean;
    note: string | null;
  };
  upcoming: MemberUpcomingEntry[];
  subscription: { id: string; planId: string; planName: string; expiresAt: string; monthlyPriceSnapshot: string; sportKeys: string[] } | null;
  // ... reste inchangé ...
}
```

2. **Select membership** (ligne ~94) : ajouter `id: true, isReferee: true, note: true` au select racine et `birthDate: true, sex: true, address: true, postalCode: true, city: true` au select `user`.

3. **Select réservations** (ligne ~107) : enrichir `participants` et ajouter `matches` :

```ts
        participants: { select: { id: true, userId: true, share: true, isOrganizer: true,
          user: { select: { firstName: true, lastName: true } } } },
        matches: {
          where: { status: { not: 'CANCELLED' } },
          orderBy: { createdAt: 'desc' }, take: 1,
          select: { winningTeam: true, sets: true, competitive: true,
            players: { select: { userId: true, team: true } } },
        },
```

4. **Mapping des rows** (fin du `.map`, ligne ~209) — compléter l'objet retourné :

```ts
      const match = r.matches[0] ?? null;
      return {
        // ... champs existants ...
        participants: r.participants.map((p) => ({
          userId: p.userId, firstName: p.user.firstName, lastName: p.user.lastName, isOrganizer: p.isOrganizer,
        })),
        match: match ? {
          winningTeam: match.winningTeam,
          myTeam: match.players.find((pl) => pl.userId === userId)?.team ?? null,
          sets: match.sets as [number, number][],
          competitive: match.competitive,
        } : null,
      };
```

5. **Nouvelles requêtes** — après le bloc « Prépayé », en `Promise.all` :

```ts
    // --- À venir + abonnement + rôle/facettes (fiche 360) ---
    const [tRegs, eRegs, lessons, subscription, staff, coachRow] = await Promise.all([
      prisma.tournamentRegistration.findMany({
        where: { OR: [{ captainUserId: userId }, { partnerUserId: userId }], status: { not: 'CANCELLED' },
          tournament: { clubId, startTime: { gt: now } } },
        select: { status: true, tournament: { select: { id: true, name: true, startTime: true } } },
      }),
      prisma.eventRegistration.findMany({
        where: { userId, status: { not: 'CANCELLED' }, event: { clubId, startTime: { gt: now } } },
        select: { status: true, event: { select: { id: true, name: true, startTime: true } } },
      }),
      prisma.lessonEnrollment.findMany({
        where: { userId, status: { not: 'CANCELLED' }, lesson: { clubId, reservation: { startTime: { gt: now } } } },
        select: { lesson: { select: { id: true, reservation: { select: { startTime: true, resource: { select: { name: true } } } } } } },
      }),
      prisma.subscription.findFirst({
        where: { clubId, userId, status: 'ACTIVE', expiresAt: { gt: now } },
        orderBy: { expiresAt: 'desc' },
        select: { id: true, planId: true, expiresAt: true, monthlyPriceSnapshot: true, sportKeys: true, plan: { select: { name: true } } },
      }),
      prisma.clubMember.findUnique({ where: { userId_clubId: { userId, clubId } }, select: { role: true } }),
      prisma.coach.findFirst({ where: { clubId, userId }, select: { id: true } }),
    ]);

    const upcoming: MemberUpcomingEntry[] = [
      ...rows.filter((r) => r.status === 'CONFIRMED' && new Date(r.startTime).getTime() > now.getTime())
        .map((r) => ({ kind: 'reservation' as const, id: r.id, title: r.resourceName, startTime: r.startTime, status: null })),
      ...tRegs.map((t) => ({ kind: 'tournament' as const, id: t.tournament.id, title: t.tournament.name,
        startTime: t.tournament.startTime.toISOString(), status: t.status })),
      ...eRegs.map((e) => ({ kind: 'event' as const, id: e.event.id, title: e.event.name,
        startTime: e.event.startTime.toISOString(), status: e.status })),
      ...lessons.filter((l) => l.lesson?.reservation).map((l) => ({ kind: 'lesson' as const, id: l.lesson!.id,
        title: `Cours · ${l.lesson!.reservation.resource.name}`, startTime: l.lesson!.reservation.startTime.toISOString(), status: null })),
    ].sort((a, b) => a.startTime.localeCompare(b.startTime)).slice(0, 5);
```

⚠️ Si `Lesson` n'a pas de champ `clubId` direct dans le where (vérifier le schéma : il l'a — `Lesson.clubId`), garder tel quel.

6. **Retour** — compléter `member` et ajouter les blocs :

```ts
      member: {
        // ... existants ...
        membershipId: membership.id,
        birthDate: membership.user.birthDate ? membership.user.birthDate.toISOString().slice(0, 10) : null,
        sex: membership.user.sex, address: membership.user.address,
        postalCode: membership.user.postalCode, city: membership.user.city,
        staffRole: staff?.role ?? null, isCoach: coachRow != null, isReferee: membership.isReferee,
        note: membership.note,
      },
      upcoming,
      subscription: subscription ? {
        id: subscription.id, planId: subscription.planId, planName: subscription.plan?.name ?? '',
        expiresAt: subscription.expiresAt.toISOString(),
        monthlyPriceSnapshot: subscription.monthlyPriceSnapshot.toString(), sportKeys: subscription.sportKeys,
      } : null,
```

- [ ] **Step 4: Vérifier le vert**

Run: `cd backend; node node_modules/jest/bin/jest.js --runTestsByPath src/services/__tests__/memberStats.service.test.ts`
Expected: PASS (anciens cas compris — compléter les fixtures existantes qui casseraient sur les nouveaux mocks manquants).

- [ ] **Step 5: Commit**

```bash
git add backend/src/services/memberStats.service.ts backend/src/services/__tests__/memberStats.service.test.ts
git commit -m "feat(members): getMemberHistory enrichi (identite complete, participants, match, upcoming, abonnement)"
```

---

### Task 5: Frontend — types API + helpers purs

**Files:**
- Modify: `frontend/lib/api.ts` (types `MemberHistory` ~1563, `UpdateMemberBody` ~1610, `MyProfile`)
- Modify: `frontend/lib/memberStats.ts`
- Test: `frontend/__tests__/memberStats.test.ts`

- [ ] **Step 1: Types API (pas de test — miroir du backend)**

Dans `frontend/lib/api.ts` :

```ts
// MemberHistoryReservation : ajouter
  participants: Array<{ userId: string; firstName: string; lastName: string; isOrganizer: boolean }>;
  match: { winningTeam: number | null; myTeam: number | null; sets: [number, number][]; competitive: boolean } | null;

// MemberHistory.member : ajouter
    membershipId: string;
    birthDate: string | null; sex: 'MALE' | 'FEMALE' | null;
    address: string | null; postalCode: string | null; city: string | null;
    staffRole: 'OWNER' | 'ADMIN' | 'STAFF' | null; isCoach: boolean; isReferee: boolean;
    note: string | null;

// MemberHistory : ajouter au niveau racine
  upcoming: Array<{ kind: 'reservation' | 'tournament' | 'event' | 'lesson'; id: string; title: string; startTime: string; status: string | null }>;
  subscription: { id: string; planId: string; planName: string; expiresAt: string; monthlyPriceSnapshot: string; sportKeys: string[] } | null;

// UpdateMemberBody : étendre
export type UpdateMemberBody = Partial<{
  isSubscriber: boolean; membershipNo: string | null; status: 'ACTIVE' | 'BLOCKED'; note: string | null;
  phone: string | null; firstName: string; lastName: string;
  birthDate: string | null; sex: 'MALE' | 'FEMALE' | null;
  address: string | null; postalCode: string | null; city: string | null;
}>;

// MyProfile : ajouter
  address: string | null;
  postalCode: string | null;
  city: string | null;
```

- [ ] **Step 2: Tests des helpers (échec d'abord)**

Dans `frontend/__tests__/memberStats.test.ts`, ajouter :

```ts
import { memberAlerts, reservationPaymentBadge, matchOutcome } from '../lib/memberStats';

describe('memberAlerts', () => {
  const base = { outstandingCents: 0, balances: [], subscriptionExpiresAt: null as string | null };
  const now = new Date('2026-07-23T10:00:00Z');
  it('reste dû → alerte coral', () => {
    expect(memberAlerts({ ...base, outstandingCents: 1200 }, now)).toContainEqual(
      expect.objectContaining({ key: 'outstanding', label: '12,00 € dus' }));
  });
  it('carnet presque vide (≤ 2 entrées)', () => {
    const balances = [{ kind: 'ENTRIES' as const, name: 'Carnet 10', creditsRemaining: 2, amountRemaining: null, expiresAt: null }];
    expect(memberAlerts({ ...base, balances }, now).map((a) => a.key)).toContain('lowPackage');
  });
  it('abonnement qui expire sous 30 jours', () => {
    expect(memberAlerts({ ...base, subscriptionExpiresAt: '2026-08-10T00:00:00Z' }, now).map((a) => a.key)).toContain('subExpiring');
    expect(memberAlerts({ ...base, subscriptionExpiresAt: '2026-12-01T00:00:00Z' }, now)).toEqual([]);
  });
});

describe('reservationPaymentBadge / matchOutcome', () => {
  it('payé / reste dû / annulée', () => {
    expect(reservationPaymentBadge({ status: 'CONFIRMED', attributedCents: 2500, dueCents: 2500 })).toEqual({ label: 'Payé 25,00 € ✓', tone: 'ok' });
    expect(reservationPaymentBadge({ status: 'CONFIRMED', attributedCents: 1300, dueCents: 2500 })).toEqual({ label: 'Reste 12,00 €', tone: 'due' });
    expect(reservationPaymentBadge({ status: 'CANCELLED', attributedCents: 0, dueCents: 2500 })).toEqual({ label: 'Annulée', tone: 'off' });
  });
  it('résultat V/D depuis le match', () => {
    expect(matchOutcome({ winningTeam: 1, myTeam: 1, sets: [[6, 3], [6, 4]], competitive: true })).toEqual({ won: true, score: '6-3 6-4' });
    expect(matchOutcome({ winningTeam: 2, myTeam: 1, sets: [[4, 6]], competitive: true })).toEqual({ won: false, score: '4-6' });
    expect(matchOutcome(null)).toBeNull();
  });
});
```

Run: `cd frontend; node node_modules/jest/bin/jest.js --runTestsByPath __tests__/memberStats.test.ts` → Expected: FAIL.

- [ ] **Step 3: Implémenter les helpers**

Dans `frontend/lib/memberStats.ts` :

```ts
export interface MemberAlert { key: 'outstanding' | 'lowPackage' | 'subExpiring'; label: string }

const fmtCents = (c: number) => `${(c / 100).toFixed(2).replace('.', ',')} €`;

/** Alertes du bandeau de la fiche 360 (occasions de relance) — pur, testé. */
export function memberAlerts(
  input: {
    outstandingCents: number;
    balances: Array<{ kind: 'ENTRIES' | 'WALLET'; name: string; creditsRemaining: number | null; amountRemaining: string | null; expiresAt: string | null }>;
    subscriptionExpiresAt: string | null;
  },
  now: Date,
): MemberAlert[] {
  const out: MemberAlert[] = [];
  if (input.outstandingCents > 0) out.push({ key: 'outstanding', label: `${fmtCents(input.outstandingCents)} dus` });
  const low = input.balances.some((b) =>
    (b.expiresAt == null || new Date(b.expiresAt).getTime() > now.getTime())
    && b.kind === 'ENTRIES' && b.creditsRemaining != null && b.creditsRemaining > 0 && b.creditsRemaining <= 2);
  if (low) out.push({ key: 'lowPackage', label: 'Carnet presque vide' });
  if (input.subscriptionExpiresAt) {
    const days = Math.ceil((new Date(input.subscriptionExpiresAt).getTime() - now.getTime()) / 86_400_000);
    if (days > 0 && days <= 30) out.push({ key: 'subExpiring', label: `Abonnement expire dans ${days} j` });
  }
  return out;
}

/** Badge paiement d'une ligne de réservation de la fiche. */
export function reservationPaymentBadge(
  r: { status: string; attributedCents: number; dueCents: number },
): { label: string; tone: 'ok' | 'due' | 'off' } {
  if (r.status === 'CANCELLED') return { label: 'Annulée', tone: 'off' };
  if (r.dueCents > 0 && r.attributedCents < r.dueCents) return { label: `Reste ${fmtCents(r.dueCents - r.attributedCents)}`, tone: 'due' };
  return { label: r.attributedCents > 0 ? `Payé ${fmtCents(r.attributedCents)} ✓` : 'Payé ✓', tone: 'ok' };
}

/** Résultat V/D du joueur sur une ligne de résa (null si aucun match saisi). */
export function matchOutcome(
  m: { winningTeam: number | null; myTeam: number | null; sets: [number, number][]; competitive: boolean } | null,
): { won: boolean; score: string } | null {
  if (!m || m.winningTeam == null || m.myTeam == null) return null;
  return { won: m.winningTeam === m.myTeam, score: m.sets.map(([a, b]) => `${a}-${b}`).join(' ') };
}
```

Note : `dueCents`/`attributedCents` sont dérivés côté page depuis `attributedAmount` (string euros) et le prix — la page utilise `toCents` de `lib/caisse`. Pour la ligne de résa, `dueCents` = part du joueur : la fiche affiche le badge à partir de `attributedAmount` seul quand la part exacte n'est pas exposée — passer `dueCents: 0` → badge « Payé ». (Le reste dû global vit déjà dans `finance.outstanding`.)

- [ ] **Step 4: Vérifier le vert + commit**

Run: `cd frontend; node node_modules/jest/bin/jest.js --runTestsByPath __tests__/memberStats.test.ts` → PASS.

```bash
git add frontend/lib/api.ts frontend/lib/memberStats.ts frontend/__tests__/memberStats.test.ts
git commit -m "feat(members): types MemberHistory etendus + helpers alertes/badges fiche 360"
```

---

### Task 6: Frontend — la liste navigue, le panneau disparaît

**Files:**
- Modify: `frontend/app/admin/members/page.tsx`
- Delete: `frontend/components/admin/members/MemberPanel.tsx`
- Test: `frontend/__tests__/AdminMembersFilters.test.tsx`, `frontend/__tests__/AdminMembersStaff.test.tsx`

- [ ] **Step 1: Adapter la page**

Dans `frontend/app/admin/members/page.tsx` :
1. Supprimer l'import `MemberPanel, MemberDraft` et l'état `selectedUserId`/`selected`.
2. `MemberRow` : `onOpen={() => router.push(`/admin/members/${m.userId}`)}` (fusionné avec `onNavigate`, prop `selected={false}` supprimée si le composant l'exige — passer `selected={false}` sinon).
3. Supprimer les fonctions `save`, `toggleBlocked`, `setRole`, `setCoach`, `setReferee` et l'état `confirmRemove` + le `ConfirmDialog` de suppression + le rendu de `MemberPanel` (desktop et mobile). La fonction `remove` et `STAFF_ERRORS` migrent dans la fiche (Task 8). Conserver `viewer`/`canManageStaff` UNIQUEMENT s'ils servent encore à autre chose dans la page (sinon supprimer aussi — vérifier avec tsc).
4. Supprimer le fichier : `git rm frontend/components/admin/members/MemberPanel.tsx`.

- [ ] **Step 2: Adapter les tests de la liste**

Run: `cd frontend; node node_modules/jest/bin/jest.js --runTestsByPath __tests__/AdminMembersFilters.test.tsx __tests__/AdminMembersStaff.test.tsx`

Les cas qui ouvraient le panneau (clic sur une ligne → panneau, save, rôle) doivent être adaptés : le clic appelle désormais `router.push('/admin/members/u1')` (asserter sur le mock `useRouter().push`). Les cas « rôle/coach/référé depuis le panneau » de `AdminMembersStaff.test.tsx` sont **déplacés** vers la suite de la fiche (Task 10) — les retirer ici en laissant un commentaire `// déplacé dans MemberHistory.test.tsx (fiche 360)`.

Expected: PASS après adaptation.

- [ ] **Step 3: Vérifier tsc + commit**

Run: `cd frontend; node node_modules/typescript/bin/tsc --noEmit` (scoper la lecture des erreurs à nos fichiers — WIP parallèle possible).

```bash
git add frontend/app/admin/members/page.tsx frontend/__tests__/AdminMembersFilters.test.tsx frontend/__tests__/AdminMembersStaff.test.tsx
git rm frontend/components/admin/members/MemberPanel.tsx
git commit -m "feat(members): clic liste = fiche directe, suppression du panneau lateral"
```

Note : entre Task 6 et Task 10, la liste navigue vers l'ancienne fiche (onglets) — état intermédiaire fonctionnel, acceptable.

---

### Task 7: Frontend — cartes du cockpit (composants)

**Files:**
- Create: `frontend/components/admin/members/MemberProfileCard.tsx`
- Create: `frontend/components/admin/members/MemberAccessCard.tsx`
- Create: `frontend/components/admin/members/MemberWalletCard.tsx`
- Create: `frontend/components/admin/members/MemberReservationsCard.tsx`
- Create: `frontend/components/admin/members/MemberOverviewCards.tsx`
- Modify: `frontend/app/globals.css`

Ces composants sont **contrôlés purs** (aucun fetch — la page fournit données + callbacks), style éditorial du site (`cardStyle`-like : surface + ombre douce, kicker petites capitales).

- [ ] **Step 1: CSS layout**

Dans `frontend/app/globals.css` :

```css
/* Fiche membre 360 — cockpit 2 colonnes (desktop), empilé (mobile) */
.mb-cols { display: flex; flex-direction: column; gap: 14px; align-items: stretch; }
@media (min-width: 900px) {
  .mb-cols { flex-direction: row; align-items: flex-start; }
  .mb-left { flex: 0 0 340px; position: sticky; top: 12px; }
  .mb-right { flex: 1; min-width: 0; }
}
.mb-grid2 { display: grid; grid-template-columns: 1fr; gap: 12px; }
@media (min-width: 640px) { .mb-grid2 { grid-template-columns: 1fr 1fr; } }
```

- [ ] **Step 2: MemberProfileCard**

```tsx
'use client';
import { useEffect, useState, CSSProperties } from 'react';
import { useTheme } from '@/lib/ThemeProvider';
import { MemberHistory, UpdateMemberBody } from '@/lib/api';

export interface ProfileDraft {
  firstName: string; lastName: string; phone: string; address: string; postalCode: string; city: string;
  birthDate: string; sex: 'MALE' | 'FEMALE' | ''; membershipNo: string;
}

export function draftFromMember(m: MemberHistory['member']): ProfileDraft {
  return {
    firstName: m.firstName, lastName: m.lastName, phone: m.phone ?? '',
    address: m.address ?? '', postalCode: m.postalCode ?? '', city: m.city ?? '',
    birthDate: m.birthDate ?? '', sex: m.sex ?? '', membershipNo: m.membershipNo ?? '',
  };
}

export function bodyFromDraft(d: ProfileDraft): UpdateMemberBody {
  return {
    firstName: d.firstName.trim(), lastName: d.lastName.trim(),
    phone: d.phone.trim() || null, address: d.address.trim() || null,
    postalCode: d.postalCode.trim() || null, city: d.city.trim() || null,
    birthDate: d.birthDate || null, sex: d.sex || null, membershipNo: d.membershipNo.trim() || null,
  };
}

export function MemberProfileCard({ member, onSave, error }: {
  member: MemberHistory['member'];
  onSave: (body: UpdateMemberBody) => Promise<void>;
  error: string | null;
}) {
  const { th } = useTheme();
  const [draft, setDraft] = useState<ProfileDraft>(() => draftFromMember(member));
  const [busy, setBusy] = useState(false);
  useEffect(() => { setDraft(draftFromMember(member)); }, [member.userId]);

  const input: CSSProperties = { border: `1px solid ${th.line}`, background: th.bg, color: th.text, borderRadius: 9, padding: '9px 10px', fontFamily: th.fontUI, fontSize: 14, width: '100%' };
  const label: CSSProperties = { fontFamily: th.fontUI, fontSize: 11, fontWeight: 700, color: th.textMute, textTransform: 'uppercase', letterSpacing: 0.3, display: 'block', margin: '10px 0 4px' };
  const set = (k: keyof ProfileDraft, v: string) => setDraft((d) => ({ ...d, [k]: v }));
  const save = async () => { setBusy(true); try { await onSave(bodyFromDraft(draft)); } finally { setBusy(false); } };

  return (
    <section aria-label="Profil" style={{ background: th.surface, borderRadius: 18, padding: 18, boxShadow: th.shadow }}>
      <h2 style={{ fontFamily: th.fontDisplay, fontWeight: 600, fontSize: 16, margin: 0, color: th.text }}>Profil</h2>
      {error && <div style={{ marginTop: 8, fontFamily: th.fontUI, fontSize: 12.5, color: th.danger }}>{error}</div>}
      <div style={{ display: 'flex', gap: 8 }}>
        <div style={{ flex: 1 }}><span style={label}>Prénom</span><input aria-label="Prénom" value={draft.firstName} onChange={(e) => set('firstName', e.target.value)} style={input} /></div>
        <div style={{ flex: 1 }}><span style={label}>Nom</span><input aria-label="Nom" value={draft.lastName} onChange={(e) => set('lastName', e.target.value)} style={input} /></div>
      </div>
      <span style={label}>Téléphone</span><input aria-label="Téléphone" value={draft.phone} onChange={(e) => set('phone', e.target.value)} style={input} />
      <span style={label}>Adresse</span><input aria-label="Adresse" value={draft.address} onChange={(e) => set('address', e.target.value)} style={input} />
      <div style={{ display: 'flex', gap: 8 }}>
        <div style={{ flex: '0 0 34%' }}><span style={label}>Code postal</span><input aria-label="Code postal" value={draft.postalCode} onChange={(e) => set('postalCode', e.target.value)} style={input} /></div>
        <div style={{ flex: 1 }}><span style={label}>Ville</span><input aria-label="Ville" value={draft.city} onChange={(e) => set('city', e.target.value)} style={input} /></div>
      </div>
      <div style={{ display: 'flex', gap: 8 }}>
        <div style={{ flex: 1 }}><span style={label}>Naissance</span><input aria-label="Date de naissance" type="date" value={draft.birthDate} onChange={(e) => set('birthDate', e.target.value)} style={input} /></div>
        <div style={{ flex: 1 }}>
          <span style={label}>Sexe</span>
          <select aria-label="Sexe" value={draft.sex} onChange={(e) => set('sex', e.target.value as ProfileDraft['sex'])} style={input}>
            <option value="">—</option><option value="MALE">Homme</option><option value="FEMALE">Femme</option>
          </select>
        </div>
      </div>
      <span style={label}>N° licence / adhérent</span><input aria-label="N° adhérent" value={draft.membershipNo} onChange={(e) => set('membershipNo', e.target.value)} style={input} />
      <div style={{ fontFamily: th.fontUI, fontSize: 11.5, color: th.textFaint, marginTop: 8 }}>L'email ({member.email}) ne peut être modifié que par le joueur.</div>
      <button onClick={save} disabled={busy} style={{ width: '100%', border: 'none', cursor: busy ? 'default' : 'pointer', borderRadius: 11, padding: 11, marginTop: 10, fontFamily: th.fontUI, fontSize: 14, fontWeight: 700, background: th.accent, color: th.onAccent, opacity: busy ? 0.5 : 1 }}>
        {busy ? 'Enregistrement…' : 'Enregistrer'}
      </button>
    </section>
  );
}
```

- [ ] **Step 3: MemberAccessCard**

Reprend les contrôles de l'ex-`MemberPanel` (Segmented rôle + hints `ROLE_HINT`, cases Coach/Juge-arbitre, case Abonné, Bloquer, Supprimer) :

```tsx
'use client';
import { CSSProperties } from 'react';
import { useTheme } from '@/lib/ThemeProvider';
import { ACCENTS } from '@/lib/theme';
import { MemberHistory } from '@/lib/api';
import { Segmented } from '@/components/ui/atoms';
import { STAFF_LABEL, StaffRole } from '@/lib/members';

type RoleSeg = 'NONE' | 'STAFF' | 'ADMIN';
const toSeg = (r: MemberHistory['member']['staffRole']): RoleSeg => (r === 'ADMIN' ? 'ADMIN' : r === 'STAFF' ? 'STAFF' : 'NONE');
const fromSeg = (s: RoleSeg): StaffRole => (s === 'NONE' ? null : s);
const ROLE_HINT: Record<RoleSeg, string> = {
  NONE: "Membre simple, pas d'accès au back-office",
  STAFF: 'Comptoir & quotidien (planning, caisse, membres, annonces)',
  ADMIN: 'Staff + structure du club (réglages, terrains, offres, comptabilité, staff, niveaux)',
};

export function MemberAccessCard({ member, viewer, canManageStaff, onSetRole, onSetCoach, onSetReferee, onSetSubscriber, onToggleBlocked, onDelete }: {
  member: MemberHistory['member'];
  viewer: { userId: string } | null;
  canManageStaff: boolean;
  onSetRole: (role: StaffRole) => void;
  onSetCoach: (v: boolean) => void;
  onSetReferee: (v: boolean) => void;
  onSetSubscriber: (v: boolean) => void;
  onToggleBlocked: () => void;
  onDelete: () => void;
}) {
  const { th } = useTheme();
  const blocked = member.status === 'BLOCKED';
  const canEditRole = canManageStaff && viewer != null && member.staffRole !== 'OWNER' && member.userId !== viewer.userId;
  const ghost: CSSProperties = { border: `1px solid ${th.line}`, background: 'transparent', cursor: 'pointer', borderRadius: 10, padding: '9px 13px', fontFamily: th.fontUI, fontSize: 13, fontWeight: 600, color: th.textMute };
  const check: CSSProperties = { display: 'flex', alignItems: 'center', gap: 9, cursor: 'pointer', fontFamily: th.fontUI, fontSize: 14, color: th.text };

  return (
    <section aria-label="Rôle et accès" style={{ background: th.surface, borderRadius: 18, padding: 18, boxShadow: th.shadow, display: 'flex', flexDirection: 'column', gap: 10 }}>
      <h2 style={{ fontFamily: th.fontDisplay, fontWeight: 600, fontSize: 16, margin: 0, color: th.text }}>Rôle &amp; accès</h2>
      {canManageStaff && (canEditRole ? (
        <>
          <Segmented<RoleSeg> value={toSeg(member.staffRole)} onChange={(s) => onSetRole(fromSeg(s))}
            options={[{ value: 'NONE', label: 'Membre' }, { value: 'STAFF', label: 'Staff' }, { value: 'ADMIN', label: 'Admin' }]} />
          <span style={{ fontFamily: th.fontUI, fontSize: 12, color: th.textMute }}>{ROLE_HINT[toSeg(member.staffRole)]}</span>
        </>
      ) : (
        <span style={{ fontFamily: th.fontUI, fontSize: 14, fontWeight: 600, color: th.text }}>{member.staffRole ? STAFF_LABEL[member.staffRole] : 'Membre'}</span>
      ))}
      {canManageStaff && (
        <>
          <label style={check}><input type="checkbox" checked={member.isCoach} onChange={(e) => onSetCoach(e.target.checked)} style={{ width: 18, height: 18, accentColor: th.accent }} /> Coach — anime des cours</label>
          <label style={check}><input type="checkbox" checked={member.isReferee} onChange={(e) => onSetReferee(e.target.checked)} style={{ width: 18, height: 18, accentColor: th.accent }} /> Juge-arbitre — pilote des tournois</label>
        </>
      )}
      <label style={check}><input type="checkbox" checked={member.isSubscriber} onChange={(e) => onSetSubscriber(e.target.checked)} style={{ width: 18, height: 18, accentColor: th.accent }} /> Abonné (fenêtre de réservation élargie)</label>
      <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap', borderTop: `1px solid ${th.line}`, paddingTop: 12 }}>
        <button onClick={onToggleBlocked} style={ghost}>{blocked ? 'Débloquer' : 'Bloquer'}</button>
        <button onClick={onDelete} style={{ ...ghost, color: ACCENTS.coral, marginLeft: 'auto' }}>Supprimer le membre</button>
      </div>
    </section>
  );
}
```

- [ ] **Step 4: MemberReservationsCard**

```tsx
'use client';
import { useTheme } from '@/lib/ThemeProvider';
import { ACCENTS } from '@/lib/theme';
import { MemberHistory } from '@/lib/api';
import { matchOutcome } from '@/lib/memberStats';

const TYPE_FR: Record<string, string> = { COURT: 'Terrain', COACHING: 'Cours', TOURNAMENT: 'Tournoi', EVENT: 'Event' };
const fmtRange = (s: string, e: string) => {
  const d = new Date(s), f = new Date(e);
  const day = new Intl.DateTimeFormat('fr-FR', { weekday: 'short', day: 'numeric', month: 'short' }).format(d);
  const hm = (x: Date) => new Intl.DateTimeFormat('fr-FR', { hour: '2-digit', minute: '2-digit' }).format(x);
  return `${day} · ${hm(d)}–${hm(f)}`;
};

export function MemberReservationsCard({ data, onSeeAll }: { data: MemberHistory; onSeeAll: () => void }) {
  const { th } = useTheme();
  const rows = data.reservations.slice(0, 5);
  const me = data.member.userId;
  return (
    <section aria-label="Dernières réservations" style={{ background: th.surface, borderRadius: 18, padding: 18, boxShadow: th.shadow }}>
      <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'baseline' }}>
        <h2 style={{ fontFamily: th.fontDisplay, fontWeight: 600, fontSize: 16, margin: 0, color: th.text }}>Dernières réservations</h2>
        <button onClick={onSeeAll} style={{ border: 'none', background: 'transparent', cursor: 'pointer', fontFamily: th.fontUI, fontSize: 12.5, fontWeight: 700, color: th.accent }}>Tout l'historique →</button>
      </div>
      {rows.length === 0 && <div style={{ fontFamily: th.fontUI, fontSize: 13, color: th.textFaint, marginTop: 10 }}>Aucune réservation.</div>}
      {rows.map((r) => {
        const cancelled = r.status === 'CANCELLED';
        const others = r.participants.filter((p) => p.userId !== me).map((p) => `${p.firstName} ${p.lastName.charAt(0)}.`);
        const result = matchOutcome(r.match);
        return (
          <div key={r.id} style={{ border: `1px solid ${th.line}`, borderRadius: 10, padding: '8px 11px', marginTop: 8, opacity: cancelled ? 0.55 : 1 }}>
            <div style={{ display: 'flex', justifyContent: 'space-between', gap: 8, flexWrap: 'wrap', fontFamily: th.fontUI, fontSize: 13, color: th.text }}>
              <span><b>{fmtRange(r.startTime, r.endTime)}</b> · {r.resourceName} <span style={{ fontSize: 10.5, fontWeight: 700, color: th.textMute, background: th.surface2, borderRadius: 5, padding: '1px 6px' }}>{TYPE_FR[r.type]}</span></span>
              <span style={{ fontWeight: 700, color: cancelled ? th.textMute : th.text }}>
                {cancelled ? (r.lateCancel ? 'Annulée · tardive' : 'Annulée') : Number(r.attributedAmount) > 0 ? `Payé ${r.attributedAmount} € ✓` : ''}
              </span>
            </div>
            <div style={{ display: 'flex', justifyContent: 'space-between', gap: 8, fontFamily: th.fontUI, fontSize: 11.5, color: th.textMute, marginTop: 2 }}>
              <span>{others.length > 0 ? `Avec ${others.join(', ')}` : ''}{r.isOrganizer ? (others.length ? ' · organise' : 'Organise') : ''}</span>
              {result && <b style={{ color: result.won ? ACCENTS.emerald : ACCENTS.coral }}>{result.won ? 'V' : 'D'} {result.score}</b>}
            </div>
          </div>
        );
      })}
    </section>
  );
}
```

(Si `ACCENTS.emerald` n'existe pas dans `lib/theme.ts`, utiliser la clé verte réelle du palette — vérifier `ACCENTS` et adapter.)

- [ ] **Step 5: MemberWalletCard et MemberOverviewCards**

`MemberWalletCard.tsx` — abonnement (actions via `SubscriptionActions`) + soldes (recharge via `PackageBalanceDialog`) ; la carte ne monte PAS les dialogs (la page les monte), elle émet :

```tsx
'use client';
import { useTheme } from '@/lib/ThemeProvider';
import { MemberHistory } from '@/lib/api';

export function MemberWalletCard({ data, onSubAction, onPkgAction }: {
  data: MemberHistory;
  onSubAction: (kind: 'renew' | 'change' | 'cancel') => void;
  onPkgAction: (mode: 'recharge' | 'adjust', bal: MemberHistory['finance']['prepaid']['balances'][number]) => void;
}) {
  const { th } = useTheme();
  const sub = data.subscription;
  const fmtDate = (iso: string) => new Intl.DateTimeFormat('fr-FR', { day: 'numeric', month: 'short' }).format(new Date(iso));
  const line = { display: 'flex', justifyContent: 'space-between', gap: 8, padding: '5px 0', borderBottom: `1px solid ${th.line}`, fontFamily: th.fontUI, fontSize: 13, color: th.text } as const;
  const ghost = { border: `1px solid ${th.line}`, background: 'transparent', cursor: 'pointer', borderRadius: 9, padding: '5px 10px', fontFamily: th.fontUI, fontSize: 12, fontWeight: 700, color: th.accent } as const;
  return (
    <section aria-label="Abonnement et soldes" style={{ background: th.surface, borderRadius: 18, padding: 18, boxShadow: th.shadow }}>
      <h2 style={{ fontFamily: th.fontDisplay, fontWeight: 600, fontSize: 16, margin: '0 0 8px', color: th.text }}>Abonnement &amp; soldes</h2>
      {sub ? (
        <>
          <div style={line}><span>{sub.planName}</span><b>→ {fmtDate(sub.expiresAt)}</b></div>
          <div style={{ display: 'flex', gap: 6, marginTop: 8, flexWrap: 'wrap' }}>
            <button style={ghost} onClick={() => onSubAction('renew')}>Renouveler</button>
            <button style={ghost} onClick={() => onSubAction('change')}>Changer</button>
            <button style={ghost} onClick={() => onSubAction('cancel')}>Résilier</button>
          </div>
        </>
      ) : <div style={{ fontFamily: th.fontUI, fontSize: 13, color: th.textFaint }}>Pas d'abonnement actif.</div>}
      <div style={{ marginTop: 10 }}>
        {data.finance.prepaid.balances.map((b) => (
          <div key={b.id} style={line}>
            <span>{b.name}</span>
            <span><b>{b.kind === 'ENTRIES' ? `${b.creditsRemaining ?? 0} rest.` : `${b.amountRemaining ?? '0.00'} €`}</b>
              {' '}<button style={ghost} onClick={() => onPkgAction('recharge', b)}>Recharger</button></span>
          </div>
        ))}
      </div>
    </section>
  );
}
```

`MemberOverviewCards.tsx` — trois petites cartes lecture seule (exportées séparément) :

```tsx
'use client';
import { useTheme } from '@/lib/ThemeProvider';
import { MemberHistory } from '@/lib/api';
import { fmtEuros, toCents } from '@/lib/caisse';
import { lastVisitLabel, cancellationLabel } from '@/lib/memberStats';

const KIND_FR: Record<string, string> = { reservation: 'résa', tournament: 'tournoi', event: 'event', lesson: 'cours' };
const STATUS_FR: Record<string, string> = { CONFIRMED: 'inscrit', WAITLISTED: 'en attente' };

function Card({ title, action, children }: { title: string; action?: React.ReactNode; children: React.ReactNode }) {
  const { th } = useTheme();
  return (
    <section aria-label={title} style={{ background: th.surface, borderRadius: 18, padding: 18, boxShadow: th.shadow }}>
      <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'baseline' }}>
        <h2 style={{ fontFamily: th.fontDisplay, fontWeight: 600, fontSize: 16, margin: 0, color: th.text }}>{title}</h2>{action}
      </div>
      <div style={{ marginTop: 8 }}>{children}</div>
    </section>
  );
}

export function MemberUpcomingCard({ data }: { data: MemberHistory }) {
  const { th } = useTheme();
  const fmt = (iso: string) => new Intl.DateTimeFormat('fr-FR', { weekday: 'short', day: 'numeric', month: 'short', hour: '2-digit', minute: '2-digit' }).format(new Date(iso));
  return (
    <Card title="À venir">
      {data.upcoming.length === 0 && <span style={{ fontFamily: th.fontUI, fontSize: 13, color: th.textFaint }}>Rien de prévu.</span>}
      {data.upcoming.map((u) => (
        <div key={`${u.kind}-${u.id}`} style={{ display: 'flex', justifyContent: 'space-between', gap: 8, padding: '4px 0', fontFamily: th.fontUI, fontSize: 13, color: th.text }}>
          <span>{fmt(u.startTime)} · {u.title}</span>
          <b style={{ color: th.textMute }}>{u.status ? STATUS_FR[u.status] ?? u.status : KIND_FR[u.kind]}</b>
        </div>
      ))}
    </Card>
  );
}

export function MemberPaymentsCard({ data, onCollect }: { data: MemberHistory; onCollect: () => void }) {
  const { th } = useTheme();
  const due = toCents(data.finance.outstanding);
  return (
    <Card title="Paiements" action={<button onClick={onCollect} style={{ border: 'none', background: 'transparent', cursor: 'pointer', fontFamily: th.fontUI, fontSize: 12.5, fontWeight: 700, color: th.accent }}>Encaisser →</button>}>
      <div style={{ fontFamily: th.fontDisplay, fontWeight: 700, fontSize: 26, color: due > 0 ? th.danger : th.text }}>
        {due > 0 ? `${fmtEuros(due)} dus` : 'Soldé ✓'}
      </div>
      <div style={{ fontFamily: th.fontUI, fontSize: 12.5, color: th.textMute, marginTop: 4 }}>Total dépensé : {fmtEuros(toCents(data.finance.totalSpent))}</div>
    </Card>
  );
}

export function MemberLoyaltyCard({ data }: { data: MemberHistory }) {
  const { th } = useTheme();
  const l = data.loyalty;
  const row = { display: 'flex', justifyContent: 'space-between', padding: '3px 0', fontFamily: th.fontUI, fontSize: 13, color: th.text } as const;
  return (
    <Card title="Fidélité & habitudes">
      <div style={row}><span>Fréquence</span><b>{l.playsPerMonth} résas/mois</b></div>
      <div style={row}><span>Dernière visite</span><b>{lastVisitLabel(l.daysSinceLastVisit)}</b></div>
      <div style={row}><span>Annulations</span><b>{cancellationLabel(l.cancellationRate)}{data.counts.lateCancelled > 0 ? ` (${data.counts.lateCancelled} tardives)` : ''}</b></div>
      {data.favorites.resource && <div style={row}><span>Habitudes</span><b>{data.favorites.resource.name}</b></div>}
    </Card>
  );
}
```

(Vérifier les signatures réelles de `lastVisitLabel`/`cancellationLabel` dans `lib/memberStats.ts` et adapter les appels.)

- [ ] **Step 6: tsc + commit**

Run: `cd frontend; node node_modules/typescript/bin/tsc --noEmit` (les composants ne sont pas encore montés — zéro erreur attendu sur nos fichiers).

```bash
git add frontend/components/admin/members/MemberProfileCard.tsx frontend/components/admin/members/MemberAccessCard.tsx frontend/components/admin/members/MemberWalletCard.tsx frontend/components/admin/members/MemberReservationsCard.tsx frontend/components/admin/members/MemberOverviewCards.tsx frontend/app/globals.css
git commit -m "feat(members): cartes du cockpit fiche 360 (profil, acces, wallet, resas, apercu)"
```

⚠️ Dev : classes CSS ajoutées à `globals.css` pendant que Turbopack tourne → chunk périmé possible ; relancer `start.ps1` avant toute vérification visuelle.

---

### Task 8: Frontend — réécriture de la page fiche (orchestrateur)

**Files:**
- Modify: `frontend/app/admin/members/[userId]/page.tsx`

- [ ] **Step 1: Réécrire la page**

Structure cible (le squelette complet ; les blocs `détail` réutilisent le JSX existant — les déplacer tels quels, ne pas les réécrire) :

```tsx
'use client';
// Imports : conserver ceux du fichier actuel + ajouter :
import { useRouter } from 'next/navigation';
import { MemberProfileCard } from '@/components/admin/members/MemberProfileCard';
import { MemberAccessCard } from '@/components/admin/members/MemberAccessCard';
import { MemberWalletCard } from '@/components/admin/members/MemberWalletCard';
import { MemberReservationsCard } from '@/components/admin/members/MemberReservationsCard';
import { MemberUpcomingCard, MemberPaymentsCard, MemberLoyaltyCard } from '@/components/admin/members/MemberOverviewCards';
import { SubscriptionActions } from '@/components/admin/subscriptions/SubscriptionActions';
import { ConfirmDialog } from '@/components/ui/ConfirmDialog'; // vérifier le chemin réel (grep ConfirmDialog)
import { memberAlerts } from '@/lib/memberStats';
import { STAFF_ERRORS, StaffRole } from '@/lib/members';
import { UpdateMemberBody, SubscriptionPlanSummary } from '@/lib/api';

type DetailTab = 'activite' | 'finances' | 'niveau' | 'fidelite';
```

États et logique de la page (en plus de l'existant `data/notes/levelData/loading/error/watch/onlyLate/noteBody/...`) :
- `detail: DetailTab | null` (null = replié ; « Tout l'historique → » → `setDetail('activite')` + scroll)
- `viewer` + `canManageStaff` : repris de la liste (chargés via `Promise.all([api.getMyClubs(token), api.getMyProfile(token)])`, même code que `frontend/app/admin/members/page.tsx:70-81`)
- `subAction: 'renew' | 'change' | 'cancel' | null` + `plans: SubscriptionPlanSummary[]` (chargés paresseusement au premier `onSubAction` via `api.adminGetSubscriptionPlans(clubId, token)` — vérifier le nom exact dans `lib/api.ts`, utilisé par `SubscriberInsights`)
- `confirmRemove: boolean` + `remove()` : migré de la liste (`api.adminRemoveMember(clubId, m.membershipId, token)` puis `router.push('/admin/members')` ; erreurs mappées `STAFF_ERRORS`)
- Actions migrées de la liste, adaptées à `membershipId`/`userId` de `data.member` :

```tsx
  const saveProfile = async (body: UpdateMemberBody) => {
    if (!token || !clubId || !data) return;
    try { setActionError(null); await api.adminUpdateMember(clubId, data.member.membershipId, body, token); await load(); }
    catch (e) { setActionError((e as Error).message === 'VALIDATION_ERROR' ? 'Vérifiez les champs saisis.' : (e as Error).message); }
  };
  const setRole = async (role: StaffRole) => { /* api.adminSetMemberStaffRole(clubId, userId, role, token) + load(), erreurs STAFF_ERRORS */ };
  const setCoach = async (v: boolean) => { /* api.adminSetMemberCoach(clubId, userId, v, token) + load() */ };
  const setReferee = async (v: boolean) => { /* api.adminSetMemberReferee(clubId, userId, v, token) + load() */ };
  const setSubscriber = async (v: boolean) => { /* api.adminUpdateMember(clubId, membershipId, { isSubscriber: v }, token) + load() */ };
  const toggleBlocked = async () => { /* api.adminSetMemberBlocked(clubId, membershipId, status !== 'BLOCKED', token) + load(), erreurs STAFF_ERRORS */ };
```

Rendu (remplace l'en-tête + `Segmented` + onglets actuels) :

```tsx
  const alerts = data ? memberAlerts({
    outstandingCents: toCents(data.finance.outstanding),
    balances: data.finance.prepaid.balances,
    subscriptionExpiresAt: data.subscription?.expiresAt ?? null,
  }, new Date()) : [];

  return (
    <div>
      <BackButton href="/admin/members" label="Membres" />

      {/* Hero identité — avatar, nom, chips, email/tel cliquables */}
      <div style={{ display: 'flex', alignItems: 'center', gap: 14, margin: '16px 0 12px', flexWrap: 'wrap' }}>
        <Avatar firstName={m.firstName} lastName={m.lastName} avatarUrl={m.avatarUrl} size={56} color={colorForSeed(m.userId)} />
        <div style={{ flex: 1, minWidth: 200 }}>
          <h1 style={{ fontFamily: th.fontDisplay, fontWeight: 600, fontSize: 30, letterSpacing: -0.5, margin: 0, color: th.text }}>{m.firstName} {m.lastName}</h1>
          <div style={{ fontFamily: th.fontUI, fontSize: 13.5, color: th.textMute, marginTop: 3 }}>
            <a href={`mailto:${m.email}`} style={{ color: 'inherit' }}>{m.email}</a>
            {m.phone && <> · <a href={`tel:${m.phone}`} style={{ color: 'inherit' }}>{m.phone}</a></>}
            {m.city && <> · {m.city}</>}
            {' '}· membre depuis {fmtDate(m.since)}
          </div>
        </div>
        <div style={{ display: 'flex', gap: 8, alignItems: 'center', flexWrap: 'wrap' }}>
          {m.isSubscriber && <Chip tone="accent">Abonné</Chip>}
          {m.hasActivePackage && <Chip tone="accent">Carnet actif</Chip>}
          <Chip tone={m.status === 'BLOCKED' ? 'line' : 'accent'}>{m.status === 'BLOCKED' ? 'Bloqué' : 'Actif'}</Chip>
          {levelEnabled && data.game.level != null && <Chip tone="mute">Niv. {data.game.level.toFixed(1)}</Chip>}
          {/* bouton 👁 À surveiller : conservé tel quel (toggleWatch existant) */}
        </div>
      </div>

      {/* Bandeau d'alertes */}
      {alerts.length > 0 && (
        <div style={{ ...dangerBanner(th), display: 'flex', gap: 14, flexWrap: 'wrap', marginBottom: 12 }}>
          {alerts.map((a) => <span key={a.key}>⚠ {a.label}</span>)}
        </div>
      )}
      {actionError && <div style={{ ...dangerBanner(th), marginBottom: 12 }}>{actionError}</div>}

      <div className="mb-cols">
        <div className="mb-left" style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
          <MemberProfileCard member={m} onSave={saveProfile} error={null} />
          <MemberAccessCard member={m} viewer={viewer} canManageStaff={canManageStaff}
            onSetRole={setRole} onSetCoach={setCoach} onSetReferee={setReferee}
            onSetSubscriber={setSubscriber} onToggleBlocked={toggleBlocked} onDelete={() => setConfirmRemove(true)} />
          {/* Carte Contact : email/tel cliquables (le bouton « Envoyer un message » arrive avec la spec messages ciblés) */}
          {/* Carte Notes : reprendre le JSX de l'onglet Notes actuel (textarea + fil + suppression confirmée) dans une <section aria-label="Notes du staff"> */}
        </div>
        <div className="mb-right" style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
          <div className="mb-grid2">
            <MemberUpcomingCard data={data} />
            <MemberWalletCard data={data} onSubAction={openSubAction} onPkgAction={(mode, bal) => setPkgAction({ mode, bal })} />
          </div>
          <MemberReservationsCard data={data} onSeeAll={() => setDetail('activite')} />
          <div className="mb-grid2">
            <MemberPaymentsCard data={data} onCollect={() => router.push('/admin/reservations')} />
            <MemberLoyaltyCard data={data} />
          </div>

          {/* Le « plus » — détails complets */}
          <Segmented<DetailTab | null> value={detail} onChange={setDetail}
            options={[
              { value: 'activite', label: 'Activité & historique' },
              { value: 'finances', label: 'Finances' },
              { value: 'niveau', label: 'Niveau' },
              { value: 'fidelite', label: 'Fidélité' },
            ]} />
          {detail === 'activite' && (/* JSX actuel de l'onglet Activité : StatCards + heatmap + table historique + filtre tardives */)}
          {detail === 'finances' && (/* JSX actuel de l'onglet Finances : graphes 12 mois, méthodes, prépayé détaillé */)}
          {detail === 'niveau' && (/* JSX actuel de l'onglet Niveau : gate levelEnabled/admin, LevelOverrideForm, courbe, corrections */)}
          {detail === 'fidelite' && (/* JSX actuel de l'onglet Fidélité : StatCards + engagement */)}
        </div>
      </div>

      {/* Dialogs : PackageBalanceDialog (existant), SubscriptionActions, ConfirmDialog suppression */}
      {subAction && data.subscription && token && clubId && (
        <SubscriptionActions action={subAction} clubId={clubId} token={token} plans={plans}
          sub={{ id: data.subscription.id, planId: data.subscription.planId, planName: data.subscription.planName,
            expiresAt: data.subscription.expiresAt, monthlyPriceSnapshot: data.subscription.monthlyPriceSnapshot }}
          onClose={() => setSubAction(null)} onDone={() => { setSubAction(null); void load(); }} />
      )}
    </div>
  );
```

Les commentaires `/* JSX actuel de l'onglet … */` = déplacer les blocs existants (`page.tsx` lignes ~233-474) sans les modifier, en retirant l'onglet Notes (devenu carte). Si `Segmented` n'accepte pas `null`, utiliser un état `detail: DetailTab` avec valeur initiale `'activite'` et rendre la rangée + le contenu sous un titre « Le plus — détails complets » toujours visible.

- [ ] **Step 2: tsc + rendu**

Run: `cd frontend; node node_modules/typescript/bin/tsc --noEmit` → zéro erreur sur nos fichiers.

- [ ] **Step 3: Commit**

```bash
git add frontend/app/admin/members/[userId]/page.tsx
git commit -m "feat(members): fiche membre 360 — cockpit 2 colonnes + details en onglets"
```

---

### Task 9: Frontend — réécriture de la suite de tests de la fiche

**Files:**
- Modify: `frontend/__tests__/MemberHistory.test.tsx`

- [ ] **Step 1: Adapter la fixture et les mocks**

La fixture `HISTORY` doit gagner les nouveaux blocs :

```ts
member: { ...existant, membershipId: 'mb1', birthDate: '1992-09-04', sex: 'FEMALE',
  address: '12 rue des Sports', postalCode: '31000', city: 'Toulouse',
  staffRole: null, isCoach: false, isReferee: false, note: null },
upcoming: [{ kind: 'tournament', id: 't1', title: 'P100 Dames', startTime: '2099-07-26T08:00:00Z', status: 'CONFIRMED' }],
subscription: { id: 's1', planId: 'pl1', planName: 'Padel illimité', expiresAt: '2099-08-10T00:00:00Z', monthlyPriceSnapshot: '39', sportKeys: ['padel'] },
```

Ajouter aux mocks `api` : `adminUpdateMember`, `adminSetMemberStaffRole`, `adminSetMemberCoach`, `adminSetMemberReferee`, `adminSetMemberBlocked`, `adminRemoveMember`, `getMyClubs` (résout `[{ clubId: 'club-1', role: 'ADMIN' }]`), `getMyProfile` (résout `{ id: 'viewer-1' }`), `adminGetSubscriptionPlans` + `next/navigation` `useRouter` avec `push: jest.fn()`.

- [ ] **Step 2: Réécrire/adapter les cas**

Garder (adaptés au nouveau rendu) : identité+à risque+carnet actif, no-show, notes (carte au lieu d'onglet), watch, niveau ADMIN/STAFF (dans le détail), recharge/correction soldes, membre introuvable. Ajouter :

```tsx
it('cockpit : profil pré-rempli et enregistrement via adminUpdateMember', async () => {
  renderPage();
  const addr = await screen.findByLabelText('Adresse');
  expect(addr).toHaveValue('12 rue des Sports');
  fireEvent.change(addr, { target: { value: '1 avenue du Club' } });
  fireEvent.click(screen.getByRole('button', { name: 'Enregistrer' }));
  await waitFor(() => expect(mockApi.adminUpdateMember).toHaveBeenCalledWith(
    'club-1', 'mb1', expect.objectContaining({ address: '1 avenue du Club', city: 'Toulouse' }), 'tok'));
});

it("bandeau d'alertes : reste dû + abonnement qui expire", async () => {
  renderPage();
  expect(await screen.findByText(/dus/)).toBeInTheDocument();
});

it('dernières réservations : ligne annulée estompée avec mention tardive', async () => {
  renderPage();
  expect(await screen.findByText(/Annulée · tardive/)).toBeInTheDocument();
});

it('rôle & accès : changer le rôle appelle adminSetMemberStaffRole', async () => {
  renderPage('ADMIN');
  fireEvent.click(await screen.findByRole('button', { name: 'Staff' }));
  await waitFor(() => expect(mockApi.adminSetMemberStaffRole).toHaveBeenCalledWith('club-1', 'u1', 'STAFF', 'tok'));
});
```

(Adapter les sélecteurs au rendu réel — `renderPage` et `AdminRoleContext` existants conservés. La fixture doit contenir une résa annulée `lateCancel: true` et un `outstanding` > 0 pour les cas alertes/annulée.)

- [ ] **Step 3: Vérifier le vert**

Run: `cd frontend; node node_modules/jest/bin/jest.js --runTestsByPath __tests__/MemberHistory.test.tsx`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add frontend/__tests__/MemberHistory.test.tsx
git commit -m "test(members): suite fiche 360 (cockpit, edition, alertes, resas detaillees)"
```

---

### Task 10: Frontend — `/me/profile` : adresse, CP, ville

**Files:**
- Modify: `frontend/lib/meProfile.ts` (buildProfileBody + UpdateProfileBody)
- Modify: `frontend/components/profile/tabs/ProfileIdentity.tsx`
- Test: `frontend/__tests__/meProfile.test.ts`, `frontend/__tests__/MeProfile.test.tsx`

- [ ] **Step 1: Tests qui échouent**

Dans `frontend/__tests__/meProfile.test.ts` :

```ts
it('buildProfileBody porte adresse/CP/ville (trim, vide → null)', () => {
  const body = buildProfileBody({ ...PROFILE, address: ' 12 rue des Sports ', postalCode: '31000', city: '' } as MyProfile);
  expect(body).toMatchObject({ address: '12 rue des Sports', postalCode: '31000', city: null });
});
```

Dans `frontend/__tests__/MeProfile.test.tsx` (onglet Identité) :

```tsx
it("l'onglet Identité expose Adresse / Code postal / Ville et les enregistre", async () => {
  renderPage();
  fireEvent.change(await screen.findByLabelText('Adresse'), { target: { value: '1 av. du Club' } });
  fireEvent.click(screen.getByRole('button', { name: /Enregistrer/ }));
  await waitFor(() => expect(mockApi.updateMyProfile).toHaveBeenCalledWith(
    expect.objectContaining({ address: '1 av. du Club' }), 'tok'));
});
```

(Vérifier le nom exact de la méthode API du PATCH profil dans le mock existant de la suite — `updateMyProfile` ou équivalent.)

Run: `cd frontend; node node_modules/jest/bin/jest.js --runTestsByPath __tests__/meProfile.test.ts __tests__/MeProfile.test.tsx` → FAIL.

- [ ] **Step 2: Implémenter**

1. `frontend/lib/meProfile.ts` — `UpdateProfileBody` + 3 champs ; `buildProfileBody` :

```ts
    address: p.address?.trim() || null,
    postalCode: p.postalCode?.trim() || null,
    city: p.city?.trim() || null,
```

⚠️ Invariant de la page : tout champ éditable DOIT figurer dans `buildProfileBody`, sinon silencieusement non-dirty et non-sauvé.

2. `ProfileIdentity.tsx` — dans la carte « Informations », après le téléphone :

```tsx
      <ProfileInput label="Adresse" value={profile.address ?? ''} onChange={(v) => set('address', v)} placeholder="12 rue des Sports" />
      <div style={{ display: 'flex', gap: 10 }}>
        <div style={{ flex: '0 0 34%', minWidth: 0 }}><ProfileInput label="Code postal" value={profile.postalCode ?? ''} onChange={(v) => set('postalCode', v)} placeholder="31000" /></div>
        <div style={{ flex: 1, minWidth: 0 }}><ProfileInput label="Ville" value={profile.city ?? ''} onChange={(v) => set('city', v)} placeholder="Toulouse" /></div>
      </div>
```

(`minWidth: 0` sur les colonnes flex — piège de débordement mobile déjà payé sur register.)

- [ ] **Step 3: Vérifier le vert + commit**

Run: `cd frontend; node node_modules/jest/bin/jest.js --runTestsByPath __tests__/meProfile.test.ts __tests__/MeProfile.test.tsx` → PASS.

```bash
git add frontend/lib/meProfile.ts frontend/components/profile/tabs/ProfileIdentity.tsx frontend/__tests__/meProfile.test.ts frontend/__tests__/MeProfile.test.tsx
git commit -m "feat(profile): adresse/CP/ville dans l'onglet Identite du profil joueur"
```

---

### Task 11: Confidentialité — tableau des données + bump PRIVACY

**Files:**
- Modify: `frontend/lib/platformContent.ts` (~ligne 207 et 223-232)
- Modify: `backend/src/content/legalVersions.ts`

- [ ] **Step 1: Mettre à jour le document**

Dans `PLATFORM_CONFIDENTIALITE` (`frontend/lib/platformContent.ts`), ligne « Compte et connexion » du tableau « Quelles données, pourquoi, combien de temps ? » :

```md
| Compte et connexion | identité, e-mail, mot de passe haché, téléphone, adresse postale | contrat | jusqu'à suppression du compte, puis anonymisation |
```

Et la ligne version : `*Version du 23 juillet 2026*`.

- [ ] **Step 2: Bump la version**

`backend/src/content/legalVersions.ts` : `PRIVACY: '2026-07-23',` (les tests de versions légales, s'ils figent la date, sont à mettre à jour — `cd backend; node node_modules/jest/bin/jest.js -t legal`).

- [ ] **Step 3: Commit**

```bash
git add frontend/lib/platformContent.ts backend/src/content/legalVersions.ts
git commit -m "docs(legal): adresse postale au tableau des donnees + bump PRIVACY 2026-07-23"
```

---

### Task 12: Vérifications finales

- [ ] **Step 1: Suites complètes ciblées**

```bash
cd backend; node node_modules/jest/bin/jest.js --runTestsByPath src/services/__tests__/club.service.test.ts src/services/__tests__/memberStats.service.test.ts src/routes/__tests__/me.routes.test.ts
cd ../frontend; node node_modules/jest/bin/jest.js --runTestsByPath __tests__/memberStats.test.ts __tests__/MemberHistory.test.tsx __tests__/AdminMembersFilters.test.tsx __tests__/AdminMembersStaff.test.tsx __tests__/meProfile.test.ts __tests__/MeProfile.test.tsx
```
Expected: tout PASS. (La suite frontend COMPLÈTE a un flake BookingModal connu — vérifier par suites ciblées + tsc.)

- [ ] **Step 2: Type-check des deux côtés**

```bash
cd backend; node node_modules/typescript/bin/tsc --noEmit
cd ../frontend; node node_modules/typescript/bin/tsc --noEmit
```
Expected: zéro erreur dans NOS fichiers (ignorer le WIP parallèle éventuel).

- [ ] **Step 3: Vérification visuelle (skill `verify`)**

Lancer la stack (`start.ps1`), puis vérifier en CDP clair + sombre, desktop 1280 + mobile 390 (`mobile:false` + width fixe — piège d'émulation connu) : la fiche d'un membre seedé (`/admin/members` → clic), édition d'un champ + Enregistrer, bandeau d'alertes, dernières résas, le « plus », et `/me/profile?tab=identite` (3 nouveaux champs). Aucun débordement horizontal.

- [ ] **Step 4: Commit final éventuel (retouches CDP)**

---

## Self-review du plan (fait à l'écriture)

- Spec couverte : migration (T1), édition admin + note (T2), joueur (T3, T10), history enrichi (T4), types+helpers (T5), navigation/panneau (T6), cockpit (T7-T8), tests fiche (T9), confidentialité (T11), vérifs (T12). La carte Contact du plan est réduite à email/tél cliquables dans le hero + placeholder — le bouton « Envoyer un message » relève de la spec messages ciblés.
- Types cohérents : `membershipId` (member), `sex: 'MALE' | 'FEMALE'`, `sets: [number, number][]`, `winningTeam` (pas winnerTeam).
- Points de vigilance signalés dans les tâches : noms exacts (`adminGetSubscriptionPlans`, `updateMyProfile`, clé verte d'`ACCENTS`, chemin `ConfirmDialog`) à vérifier au moment du câblage — signalés là où ils s'utilisent.
